#include "services/auth/auth_service.h"

// Implementation is fully inline in the header for simplicity.
// This .cpp file is required by CMake but can host future non-inline logic
// such as database lookups, account persistence, etc.
